---
version: "1.1.3"
date: "2026-03-14T00:00:00Z"
---

## Fixed

- **TAB key restored** — vault ghost suggestions no longer intercept Tab; Tab always reaches the PTY for shell tab-completion. Right Arrow continues to accept the current suggestion.
- **Ghost persists after vault disable** — `updateVaultSuggestion` now clears the ghost when `vault == nil`, so disabling vault via config (including hot-reload) immediately removes stale ghost text.
